# 规则检查：不输出仓位建议

检查时间：2026-06-11

## 对照规则

- R004：只读阶段，不连接钱包、不下单。
- R009：中文优先展示。
- R013：用户可见层只输出下注方向和分析结果，不输出仓位建议、下注金额、下注单位或今日额度。

## 本次处理

- 主页预测事件决策榜改为展示方向、关注、忽视、盈亏比、风险和分析说明。
- 预测市场列表页保留方向、结构、状态、价格、分析和跳转，不展示下注数量。
- 机会层 CSV 移除 `recommended_stake_units` 字段。
- 每日决策榜 CSV 移除 `recommended_stake_units` 字段。
- Markdown 摘要改为中文，并明确只输出方向和分析。
- Polymarket 跳转继续使用 `?via=serene77mc-g6kj`。

## 仍保留

底层数据库和内部模型可以保留历史 staking 表作为评分输入，但不得直接展示给用户。
