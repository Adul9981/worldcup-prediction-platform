# 规则检查：前台页面瘦身与后台隔离执行

检查时间：2026-06-12

## 本次执行

- 今日页只保留：
  - 今日推荐
  - 今日比赛策略
  - 今日比赛
- 预测事件库页只保留：
  - 事件总览
  - 筛选
  - 完整事件表
- 进度纵览页只保留：
  - 阶段进度
  - 全部比赛状态
- 工作台降级为简短入口页。
- 全部市场页改为原始数据查看页，不再作为前台主入口。
- 前台页面移除后台管理入口。
- 后台策略录入增加格式校验。

## 已删除或隐藏的内容

- 今日页删除指标卡、今日事件清单、当前已上架机会、准入状态、今日规则。
- 事件库删除今日比赛、低风险事件、高风险事件、进度纵览、可选预测策略等中间模块。
- 进度页删除当前市场结构、当前方向分布、赛程阶段统计、阶段交易重点。
- 前台导航不再暴露后台管理。

## 时间口径

- 今日页默认北京时间。
- 进度页默认北京时间。
- 缺少精确开球时间时显示“开球时间待确认”。
- 进度页比赛状态显示：已结束、今日、后续、时间待确认。

## 后台校验

后台策略保存前校验：

- 标题或正文至少一个不为空。
- 必须有推荐等级。
- 策略正文至少包含理由、方向、取消条件中的两类。
- 不得包含仓位、下注金额、下注单位、今日额度、加仓、减仓。
- Polymarket 链接自动补邀请码。

## 验证结果

- `node --check site/today.js` 通过。
- `node --check site/events.js` 通过。
- `node --check site/progress.js` 通过。
- `node --check site/admin.js` 通过。
- `python3 tools/site/build_release.py` 通过。
- 浏览器验证：今日页无数据加载失败，只保留三个核心模块。
- 浏览器验证：事件库页无数据加载失败，只保留总览和完整事件表。
- 浏览器验证：进度页无数据加载失败，显示已结束、今日、后续状态。
- 浏览器验证：前台页面无后台管理入口。
