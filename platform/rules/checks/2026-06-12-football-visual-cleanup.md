# 规则检查：前台视觉增强与提示文案清理

检查时间：2026-06-12

## 用户要求

- 网站展示太朴素，需要增加足球或世界杯元素。
- 删除类似“后台手动维护的重点策略，优先于模型自动清单展示”“只读版本”等提示备注文字。

## 本次规则入库

- 新增 R029：前台减少解释性提示，增加克制的世界杯视觉。

## 本次实现

- 今日页删除：
  - `只读版本`
  - `后台手动维护的重点策略，优先于模型自动清单展示`
  - footer 解释文字
- 后台页删除：
  - `本地管理`
  - `保存后会置顶显示...`
  - 后台说明区
  - footer 解释文字
- 事件库和进度页删除 `只读版本` 等状态提示。
- 增加球场绿色顶部背景、草坪网格背景、固定导航和比赛卡片视觉。
- 比赛卡片改为更接近赛程卡片的样式。

## 验证

- `node --check site/today.js` 通过。
- `node --check site/events.js` 通过。
- `node --check site/progress.js` 通过。
- `node --check site/admin.js` 通过。
- `python3 tools/site/build_release.py` 通过。
- 本地文件扫描未发现目标提示文字残留。
