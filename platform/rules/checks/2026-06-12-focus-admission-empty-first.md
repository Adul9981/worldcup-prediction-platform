# 规则检查：重点清单准入优先

检查时间：2026-06-12

## 用户要求

- 当前低风险、高风险、每日关注的事件筛选是错误的。
- 先完善规则库，再往清单里填预测事件。
- 宁可空着，不要乱加乱写。

## 本次规则更新

- 新增 R020：重点清单必须人工准入，默认空置。
- 每日关注、今日比赛策略、低风险事件、高风险事件、可选预测策略不得由模型方向、球队强弱、价格、回报率、交易量、流动性或模板事件自动填充。
- 所有新生成事件默认：
  - `focus_bucket = 未入选`
  - `focus_admission_note = 未经过人工准入`

## 本次实现

- 预测事件库新增字段：
  - `focus_bucket`
  - `focus_admission_note`
- 事件生成脚本停止自动输出：
  - 低风险候选
  - 低风险待监控
  - 高风险候选
  - 高风险待监控
  - 可选策略
- 今日页和事件库页只展示通过准入的重点事件。
- 没有准入事件时显示空状态，不用冠军盘、强弱模板、阶段到达模板、高回报模板补位。

## 当前数据结果

- 预测事件总数：327。
- `focus_bucket = 未入选`：327。
- 入选每日关注：0。
- 入选低风险：0。
- 入选高风险：0。
- 入选可选策略：0。

## 验证结果

- `python3 tools/polymarket/update_pipeline.py` 通过。
- `node --check site/events.js` 通过。
- `node --check site/today.js` 通过。
- `python3 tools/site/build_release.py` 通过。
- 浏览器验证：今日页显示“暂无通过准入的每日关注事件”。
- 浏览器验证：今日页显示“暂无通过准入的可选策略”。
- 浏览器验证：事件库页显示低风险、高风险、每日关注均为空状态。
- 浏览器验证：事件库页“入选重点”为 0。
- 浏览器验证：无数据加载失败。
