# 规则检查：线上今日推荐后台

检查时间：2026-06-14。

## 对照规则

- R025：后台管理必须独立隔离，不能从用户前台发现。
- R028：今日推荐只来自用户后台手动录入。
- R029：前台减少解释性提示，增加克制的世界杯视觉。
- R030：今日推荐后台必须使用线上共享数据源。

## 检查结果

- 后台仍为独立入口 `/site/admin.html`，前台导航不暴露后台链接。
- 后台已取消格式校验，只要求标题或正文不为空。
- 后台保存改为调用 `/api/recommendations`，并使用 `ADMIN_TOKEN` 管理口令。
- 前台今日页优先读取 `/api/recommendations`，不再优先读取当前浏览器 `localStorage`。
- 若线上 KV 存储未配置，API 返回失败，后台显示错误，不假装发布成功。
- 所有页面已引用 `site/favicon.svg`。

## 当前阻塞

- Vercel 项目尚未连接 KV/Upstash Redis 存储。
- 需要生成并配置 `KV_REST_API_URL` 和 `KV_REST_API_TOKEN` 后，跨浏览器共享才会正式生效。

## 结论

代码层通过；线上共享能力待 Vercel 存储连接完成后复验。
